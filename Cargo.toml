[package]
name = "forge-sensor"
version = "0.1.0"
edition = "2021"
description = "Sensor data decomposition into tiles for Plato agents"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde"] }
